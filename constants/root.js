const ROOT__PRODUCTS = document.getElementById('products');
const ROOT__HEADER = document.getElementById('header');
const ROOT__CART = document.getElementById('cart');
const ROOT__SPINNER = document.getElementById('spiner');
